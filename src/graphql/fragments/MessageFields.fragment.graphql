# An example fragment created for messages table fields:
# !NOTE: You should to run codegen first before using this graphql hook
# *Codegen will generate a fragment like this one => MessageFragment
#
# *This is an example usage:
#
# type MessageModel = {
#   message: MessageFragment
# }
#

fragment MessageFields on messages {
  id
  msg
}
